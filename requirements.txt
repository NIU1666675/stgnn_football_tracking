# Dependències mínimes per executar el codi del projecte
# (multi_head_model/ + stgcn_tracking/).
#
# Instal·lació recomanada:
#   pip install -r requirements.txt
#
# Per a entrenament en GPU, instal·la PyTorch amb el wheel adequat per a
# la teva versió de CUDA des de https://pytorch.org/get-started/locally/

torch>=2.5
numpy>=2.0
pandas>=2.0
matplotlib>=3.7

# Accelera 3-5× el parseig dels tracking_extrapolated.jsonl (cada partit té
# ~60-90k línies JSON). Si no està instal·lat, es cau al `json` estàndard.
orjson>=3.9
